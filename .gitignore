# Godot 4+ specific ignores
.godot/
*.translation
*.pck
# export_presets.cfg viene generato dalla CI (vedi .github/workflows/deploy.yml).
# Se in locale lo crei a mano, rimuovi la riga sotto e committalo.
export_presets.cfg

# macOS
.DS_Store

# Export output directories
/build/
/export/
